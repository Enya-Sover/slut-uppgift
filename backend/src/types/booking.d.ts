


interface Booking {
    id: string,
    createdAt: string,
    checkInDate: string,
    checkOutDate: string,
    totalPrice: number,
    user_id: string,
    property_id: string
}

//Fält: id, createdAt, checkInDate, checkOutDate, totalPrice
